import Link from "next/link";
import type { ReactNode } from "react";
export function ButtonLink({href,children,external=false,variant="primary",className=""}:{href:string;children:ReactNode;external?:boolean;variant?:"primary"|"secondary"|"whatsapp";className?:string}){const cls=`inline-flex min-h-11 items-center justify-center rounded-md px-5 py-3 text-sm font-black shadow-sm ${variant==="whatsapp"?"bg-brand-green text-white":variant==="secondary"?"bg-white text-brand-blue ring-1 ring-slate-200":"bg-brand-blue text-white"} ${className}`;return external?<a href={href} target="_blank" rel="noreferrer" className={cls}>{children}</a>:<Link href={href} className={cls}>{children}</Link>}
